
Name: backend-api
Host: 0.0.0.0    # 后端ip
Port: 8443       # 后端port，绑定tg webhook，不能变
Timeout: 100000  # 超时

Log:
  Level: debug   
  KeepDays: 30

Auth:
  AccessSecret: bq0536f9-6P50-5T06-8e13-pvOjleU0PCIN

Mysql:
  # 数据库配置： 用户名:密码@(ip:端口)/库名？超时设置
  DataSource: admin:YHt6_8YeHiG_C?ixZP@tcp(127.0.0.1:3306)/savvy_gameing?timeout=1s&readTimeout=1s&writeTimeout=1s&charset=utf8mb4&parseTime=true&loc=Local
  MaxIdleConns: 10
  MaxOpenConns: 100
  ConnMaxLifetime: 3600

BizRedis:
  Host: 127.0.0.1:6379   # ip:port
  Pass: ''               # redis.conf 设置密码
  Type: node             # 默认

CacheRedis:
  - Host: 127.0.0.1:6379  # ip:port
    Pass: ''              # redis.conf 设置密码
    Type: node            # 默认

TgWebHook: https://game-invite.vercel.app:8443
TgPublicPem: etc/YOURPUBLIC.pem
TgPrivateKey: etc/YOURPRIVATE.key
